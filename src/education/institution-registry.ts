// @ts-nocheck
export function educationInstitution({institutionId, name, level, regionCode, accreditation='UNKNOWN'}={}) {
  return {institutionId,name,level,regionCode,accreditation,status:'ACTIVE'};
}
