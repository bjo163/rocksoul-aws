services:
  cosmic-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: cosmic:local
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:3000"
