export function registerLegacyTypePack(runtime: { typeRegistry: { register(input: unknown): unknown } }, { typeId, entityFamily, handler, source = 'legacy' }: { typeId?: string; entityFamily?: string; handler?: { name?: string }; source?: string } = {}) { return runtime.typeRegistry.register({ typeId, entityFamily, handlerName: handler?.name ?? null, source, adapter: 'LEGACY_BRIDGE', version: 1 }); }
