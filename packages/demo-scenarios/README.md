# Demo scenarios

Shared scenario definitions for reproducible local/demo data and API/browser E2E validation.

Scenarios: `basic`, `review`, `full`.

Production `db:install` must remain separate from demo loading.
